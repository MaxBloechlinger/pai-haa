# Notes

## Phase 0

- Built a minimal in-memory inverted index for three hardcoded documents.
- Stored postings as document ID lists keyed by normalized term.
- Added a smoke test for `Search(term)` and a tiny `main` for `go run .`.
